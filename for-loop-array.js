
// run a loop for each element of an array !!

var vegis = ['porota', 'kapali', 'mama', 'lala', 'komola'];

for(let i = 0; i < vegis.length; i++ ){
    const vegi = vegis[i];
    console.log(vegi);
}