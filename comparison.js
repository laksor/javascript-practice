//  special character as comp
// as : = , == , != , > , < && , || 

// value :
var first = 5;

var second = 6;

// example as using comparison symbol :

var comp = first > second;
var comp = first > second;
var comp = first == second;
var comp = first != second;
var comp = first && second;
var comp = first || second;

// getting output using :

console.log(comp);